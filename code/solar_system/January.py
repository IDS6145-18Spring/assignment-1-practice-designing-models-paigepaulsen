import math
from schedule import schedule

class January(schedule):
	'''this is the month of January'''
	def __init__(self, oa, t, p, name):
		schedule(30, 9, p, January) #this assumes that the sun only moves 1 degree north to south every month 

    def whatmonth():
        if timestep % 12 == 0 #timestep should be in main, like in POTS, right?
            return January(schedule) # is this how I set this?
            return self.p = sun.power #should read from sun file, or run, better, run the shine() function inside the sun class. How do I make it do that?
            print str(whatmonth(self))
        else 
        	return None

