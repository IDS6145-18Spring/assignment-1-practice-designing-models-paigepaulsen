import math
from schedule import schedule

class March(schedule):
	'''this is the month of January'''
	def __init__(self, oa, t, p, name):
		schedule(30, 10.00, p, March) #this assumes that the sun only moves 1 degree north to south every month 

    def whatmonth():
        if timestep % 12 == 2 #timestep should be in main, like in POTS, right? So I call a response to the timestep in a subclass, yes?
            return March(schedule) # is this how I set this?
            return self.p = sun.power 
            print str(whatmonth(self))
        else 
        	return None	