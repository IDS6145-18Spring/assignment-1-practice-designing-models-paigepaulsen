import math
from schedule import schedule

class October(schedule):
	'''this is the month of January'''
	def __init__(self, oa, om, oe, name):
		schedule(30, 7.00, 12.00, October) #this assumes that the sun only moves 1 degree north to south every month 

    def whatmonth():
        if timestep % 12 == 9 #timestep should be in main, like in POTS, right?
            return October(schedule) # is this how I set this?
            print str(whatmonth(self))
        else 
        	return None	