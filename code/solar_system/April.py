import math
from schedule import schedule

class April(schedule):
	'''this is the month of January'''
	def __init__(self, oa, om, oe, name):
		schedule(30, 7.00, 12.00, April) #this assumes that the sun only moves 1 degree north to south every month 

    def whatmonth():
        if timestep % 12 == 3 #timestep should be in main, like in POTS, right?
            return April(schedule) # is this how I set this?
            print str(whatmonth(self))
        else 
        	return None	